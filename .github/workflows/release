name: Release

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yml

  test:
    uses: ./.github/workflows/test.yml
    needs: build

  bump-version:
    uses: ./.github/workflows/tag-bump.yml
    needs: [build, test]
